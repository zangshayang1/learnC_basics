#ifndef _stack_h
#define _stack_h

extern int THE_SIZE; // extern keyword: declared and defined separately, could be accessed from outside.

int get_age();
void set_age(int age);

double update_ratio(double ratio);

void print_size();

#endif
